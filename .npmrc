# 本文件刻意**不设置 registry**。
#
# 这里曾经钉着 registry=https://registry.npmmirror.com/（国内只读镜像），代价是：
#   - `pnpm audit` 对所有人失效：镜像没有 advisories/bulk 端点
#     （ERR_PNPM_AUDIT_ENDPOINT_NOT_EXISTS）
#   - 发布链路要靠三层保险绕开它，CI 六个 job 各自 sed 改写本文件
#   - 海外贡献者被强制走一个他们够不到、且不保证同步的源
#
# 镜像属于**开发者个人环境**，写在 ~/.npmrc 即可，不该随开源库分发：
#   npm config set registry https://registry.npmmirror.com/ --location=global
#
# 见 CONTRIBUTING.md「使用镜像源」。
