# Word Import And Export

Word import/export tools are part of the full preset toolbar.

```vue
<YanivEditor preset="full" />
```

Use `appearance="word"` for the Word-like visual skin:

```vue
<YanivEditor preset="full" appearance="word" />
```
