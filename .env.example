# Yaniv Editor — 构建期 AI 配置（仅本地开发 / demo）
# 复制为 .env 后填写。这是 AI 配置解析链的**最后一级**：
# 宿主的 `ai-config` prop 与 AI 设置弹窗写入的 localStorage 都优先于它。
# 生产集成请用 `ai-config` + storageMode: "proxy"，不要把密钥打进产物。

# AI Provider: openai | deepseek | aliyun | ollama | custom
VITE_AI_PROVIDER=openai

# API Key（openai / deepseek / aliyun / custom 需要；ollama 不需要）
VITE_AI_API_KEY=your-api-key-here

# Base URL（可选，缺省用各 provider 的默认端点，见 src/features/ai/config/types.ts）
# OpenAI:   https://api.openai.com/v1
# DeepSeek: https://api.deepseek.com
# Aliyun:   https://dashscope.aliyuncs.com/compatible-mode/v1
# Ollama:   http://localhost:11434/api
# custom 必须显式给出（无默认值）
# VITE_AI_BASE_URL=

# Model（可选，缺省用各 provider 的默认模型）
# OpenAI:   gpt-4o-mini（默认）、gpt-4o
# DeepSeek: deepseek-chat（默认）
# Aliyun:   qwen-plus（默认）、qwen-turbo、qwen-max
# Ollama:   llama3.2（默认）、mistral、codellama
# custom 必须显式给出（无默认值）
# VITE_AI_MODEL=

# 模型调参。宿主 `ai-config` 与 AI 设置弹窗都不带这两项，因此它们**不跟随凭据来源分级**：
# 无论最终用哪一级的 key / endpoint，调参都取这里的值（缺省 0.7 / 2048，填非数字也退回缺省）。
# VITE_AI_TEMPERATURE=0.7
# VITE_AI_MAX_TOKENS=2048

# Demo mode：未配置可用密钥时用模拟流式输出代替真实请求（仅本地开发）
# VITE_AI_DEMO_MODE=true
